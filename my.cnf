[mysqld]
# Set memory limits
innodb_buffer_pool_size=512M
key_buffer_size=64M

# Connection settings
max_connections=200

# Logging
slow_query_log=1
slow_query_log_file=/var/log/mysql_slow.log
long_query_time=2
